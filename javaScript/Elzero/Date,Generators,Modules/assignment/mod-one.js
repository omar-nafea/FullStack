import { a, b, c } from "./mod-two.js";
let numOne = a
let numTwo = b
let numThree = c
export { numOne, numTwo, numThree };
export default function(numun, numdeux, sisco) {
    return numun + numdeux + sisco;
}